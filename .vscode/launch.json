{
    "version": "2.0.0",
    "configurations":[
        {
            "name": "Run ArcadeEngine (Debug)",
            "type": "cppdbg",
            "MIMode": "gdb",
            "request": "launch",
            "program": "${workspaceFolder}/Build/ArcadeEngine",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}