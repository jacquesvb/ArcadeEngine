{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build ArcadeEngine (Debug)",
            "type": "shell",
            "command": "cmake",
            "args": ["--build", "Build", "--config", "Debug", "-j"],
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}